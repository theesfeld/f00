name: FreeBSD smoke

# Product target is freestanding Linux x86-64 ASM. FreeBSD is not supported.
# Manual only — do not burn CI minutes on every push.

on:
  workflow_dispatch:

jobs:
  freebsd:
    name: FreeBSD (not a product target)
    runs-on: ubuntu-latest
    steps:
      - name: Documented non-target
        run: |
          echo "f00tils product surface is pure freestanding Linux x86-64 assembly."
          echo "FreeBSD / Darwin ports are future work."
          exit 0
