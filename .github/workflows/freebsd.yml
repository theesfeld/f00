name: FreeBSD smoke

# Product is freestanding Linux x86-64 ASM first. FreeBSD is not a target yet.
# Keep the workflow file so historical links work; job is intentionally skipped.

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  freebsd:
    name: FreeBSD (skipped — Linux ASM first)
    runs-on: ubuntu-latest
    steps:
      - name: Not a FreeBSD target yet
        run: |
          echo "f00 v0.15+ product surface is pure freestanding Linux x86-64 assembly."
          echo "FreeBSD / Darwin ports are future work — see README."
          exit 0
