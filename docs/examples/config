# Example: copy to ~/.config/f00/config
# See docs/CONFIG.md

core = false
color = auto
icons = auto      # Nerd File Icons when color on; ascii on linux console
# icons = ascii   # no Nerd Font installed
# icons = never
# icons = nerd    # force Nerd (install a Nerd Font in the terminal)
# icons = emoji
# icons = glyph
animations = true
spinner = true

[ls]
git = true
icons = auto

[cat]
# future util-specific knobs

[sha256sum]
spinner = true
