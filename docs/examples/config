# Example: copy to ~/.config/f00/config
# See docs/CONFIG.md

core = false
color = auto
icons = auto      # modern: 1-cell type gutter (d/l/x/-) when color on
# icons = never   # color/table only
# icons = glyph   # Unicode glyph skin
# icons = emoji   # emoji skin
# icons = nerd    # Nerd Font PUA
# icons = ascii   # force ascii letters
animations = true
spinner = true

[ls]
git = true
icons = auto

[cat]
# future util-specific knobs

[sha256sum]
spinner = true
