# Example: copy to ~/.config/f00/config
# See docs/CONFIG.md

core = false
color = auto
icons = never     # default: color + tables, no prefix icons
# icons = ascii   # opt-in 1-cell d/l/x/-
# icons = glyph
# icons = emoji
# icons = nerd
animations = true
spinner = true

[ls]
git = true
icons = auto

[cat]
# future util-specific knobs

[sha256sum]
spinner = true
