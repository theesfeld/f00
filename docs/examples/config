# Example: copy to ~/.config/f00/config
# See docs/CONFIG.md

core = false
color = auto
icons = auto
# icons = emoji   # force emoji (default style)
# icons = nerd    # Nerd Font PUA (needs patched font)
# icons = ascii   # [D]/[F]/… always works
animations = true
spinner = true

[ls]
git = true
icons = auto

[cat]
# future util-specific knobs

[sha256sum]
spinner = true
